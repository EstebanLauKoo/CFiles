#include <stdio.h>
//Compiler directive (tells the compiler to include stdio(standard IO) library.

int main2() {
    int x = 10;
    int y = 3;
    double z;

    z= (double)x/y;
    printf("Hi guys my name is Esteban, And I like to work out. \n");
    printf("x int is %i, y int is %i, z double is %lf",x,y,z);
    return 0;
}

